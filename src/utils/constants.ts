import { AppMode } from '../types';

export const APP_NAME = "Athera AI";
export const VERSION = "V1";

export const DEFAULT_MODE = AppMode.AGENT;

export const INITIAL_WELCOME_MESSAGE = "Athera — Ready";
